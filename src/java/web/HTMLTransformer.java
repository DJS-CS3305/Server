package web;

/**
 * Class for translating to and from the pseudo-HTML used by the admin client
 * and regular HTML for the website.
 * 
 * @author Stephen Fahy
 */
public class HTMLTransformer {
    /**
     * Translates pseudo-HTML to normal HTML.
     * @param pseudo Pseudo-HTML.
     * @return Translated normal HTML.
     */
    public static String toHTML(String pseudo) {
        return "";
    }
    
    /**
     * Translates HTML to pseudo-HTML.
     * @param html HTML.
     * @return Translated pseudo-HTML.
     */
    public static String toPseudo(String html) {
        return "";
    }
}
